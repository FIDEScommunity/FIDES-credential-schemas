{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://fidescommunity.github.io/FIDES-credential-schemas/schemas/iscc/role/CFP.roleCredential.schema.json",
  "title": "CFP Role Credential",
  "description": "Domain model for a Clean Fuel Protocol Role Credential. This credential asserts one or more supply-chain roles for an organization. It follows the FIDES JSON Schema pattern: only credentialSubject fields are defined here.",
  "type": "object",
  "properties": {
    "vc": {
      "type": "string",
      "const": "urn:org:cfp.role:1",
      "description": "Data model identifier for the CFP Role Credential."
    },
    "cfpRoles": {
      "type": "array",
      "description": "One or more Clean Fuel Protocol roles assigned to the organization.",
      "items": {
        "type": "string",
        "enum": [
          "farmCropProducer",
          "millProcessingUnit",
          "collectorPointOfOrigin",
          "trader",
          "logistics",
          "biofuelProducer",
          "fuelSupplier",
          "regulator",
          "schemeProvider",
          "certificationBody"
        ]
      },
      "minItems": 1,
      "uniqueItems": true,
      "example": [
        "trader",
        "logistics"
      ]
    }
  },
  "required": [
    "type",
    "cfpRoles"
  ]
}
